# AI GM

An iOS application integrated with Firebase.

## Setup

1. Clone the repository.
2. Ensure you have your `GoogleService-Info.plist` added to the `AI GM/` directory.
3. Open the `.xcodeproj` in Xcode and run the project.
